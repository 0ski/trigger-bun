{
  "name": "references-bun-catalog",
  "devDependencies": {
    "@types/bun": "^1.1.6"
  },
  "private": true,
  "scripts": {
    "dev:trigger": "trigger dev",
    "deploy": "trigger deploy"
  },
  "type": "module",
  "dependencies": {
    "@trigger.dev/sdk": "^4.4.1",
    "trigger.dev": "^4.4.1"
  }
}